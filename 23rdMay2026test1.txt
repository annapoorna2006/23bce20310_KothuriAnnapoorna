use task1;
CREATE TABLE Departments (
dept_id INT PRIMARY KEY,
dept_name VARCHAR(50) NOT NULL,
location VARCHAR(50)
);
INSERT INTO Departments (dept_id, dept_name, location) VALUES
(10, 'Finance', 'New York'),
(20, 'IT', 'San Francisco'),
(30, 'Sales', 'Chicago'),
(40, 'Marketing', 'Los Angeles');

CREATE TABLE Jobs (
job_id INT PRIMARY KEY,
job_title VARCHAR(50),
min_salary DECIMAL(10,2),
max_salary DECIMAL(10,2)
);
INSERT INTO Jobs (job_id, job_title, min_salary, max_salary) VALUES
(1, 'Manager', 80000.00, 150000.00),
(2, 'Developer', 60000.00, 110000.00),
(3, 'Analyst', 50000.00, 90000.00),
(4, 'Intern', 30000.00, 45000.00);

CREATE TABLE Employees (
emp_id INT PRIMARY KEY,
name VARCHAR(50) NOT NULL,
manager_id INT,
dept_id INT,
job_id INT,
salary DECIMAL(10,2),
hire_date DATE,
FOREIGN KEY (dept_id) REFERENCES Departments(dept_id),
FOREIGN KEY (job_id) REFERENCES Jobs(job_id)
);
INSERT INTO Employees (emp_id, name, manager_id, dept_id, job_id, salary, hire_date) VALUES
-- Top Level Manager
(501, 'Alice', NULL, 10, 1, 120000.00, '2015-01-01'),
-- IT Dept Staff
(503, 'Charlie', 501, 20, 1, 95000.00, '2016-05-20'),
(504, 'David', 503, 20, 2, 85000.00, '2019-11-01'),
(505, 'Eve', 503, 20, 2, 78000.00, '2020-02-15'),
-- Finance Dept Staff
(502, 'Bob', 501, 10, 3, 55000.00, '2018-03-12'),
-- Sales Dept Staff
(506, 'Frank', 501, 30, 3, 52000.00, '2021-06-21'),
-- Marketing (unassigned to manager for complexity)
(507, 'Grace', NULL, 40, 2, 72000.00, '2022-08-10');

--- 1st question ---
select e.name, j.job_title, d.dept_name from Employees e join Jobs j on e.job_id = j.job_id join Departments d on e.dept_id = d.dept_id;
--- 3rd question ---
SELECT d.dept_name, SUM(e.salary) AS total_salary FROM Employees e JOIN Departments d ON e.dept_id = d.dept_id GROUP BY d.dept_name;
--- 4th question ---
-- 4
select d.dept_name, count(*) as total_employees, avg(e.salary) as avg_salary from employees e join departments d on e.dept_id = d.dept_id group by d.dept_name having count(*) > 2 and avg(e.salary) > 70000;
--- 5th question---;
select e.name as employee_name, m.name as manager_name from employees e left join employees m on e.manager_id = m.emp_id;
--- 6th question---;
select name, salary from employees order by salary desc limit 3;
--- 7th question---;
select e.name from employees e join jobs j on e.job_id = j.job_id join departments d on e.dept_id = d.dept_id where j.job_title = 'Developer' and d.location = 'San Francisco';
--- 8th questiom---;
select year(hire_date) as hire_year, count(*) as total_employees from employees group by year(hire_date);
--- 9th question ---
select j.job_title, max(e.salary) as max_salary from employees e join jobs j on e.job_id = j.job_id group by j.job_title having max(e.salary) < 100000;
--- 10th question ---
select name from employees where dept_id = (
    select dept_id
    from employees
    where name = 'David'
);

--- 11th question ---
select d.dept_name, e.name from departments d left join employees e on d.dept_id = e.dept_id;

--- 14th question ---
select j.job_title, e.salary from employees e join jobs j on e.job_id = j.job_id where e.emp_id = 504;

--- 15th quetsion---
select manager_id, min(salary) as minimum_salary from employees where manager_id is not null group by manager_id;

--- 17th question ---
select max(salary) - min(salary) as salary_difference from employees;

--- 18yh question---
select e.name, e.hire_date, d.dept_name, e.salary from employees e join departments d on e.dept_id = d.dept_id order by d.dept_name asc, e.salary desc;

--- 19th question ---
select manager_id, count(*) as total_employees from employees where manager_id is not null group by manager_id having count(*) > 2;

--- 20th question---
select name, salary from employees where salary = (
    select distinct salary
    from employees
    order by salary desc
    limit 1 offset 2
);

--- 21 st question ---
select j.job_title, count(e.emp_id) as total_employees from jobs j left join employees e on j.job_id = e.job_id group by j.job_title;

--- 22nd question ---
select e.name from employees e join departments d on e.dept_id = d.dept_id where d.dept_name = 'Finance' and e.name like '%i%';

--- 23rd question---
select d.dept_name, sum(e.salary * 0.10) as total_bonus from employees e join departments d on e.dept_id = d.dept_id group by d.dept_name;

--- 25th question ---
select e.name from employees e join employees m on e.manager_id = m.emp_id where m.name = 'Charlie';